import Gradients from '@/components/landing/Gradients';
import React from 'react'

export default function Home() {
  return (
    <div className='relative flex flex-col items-center justify-center h-screen overflow-hidden'>
      <Gradients/>
    </div>
  );
}
